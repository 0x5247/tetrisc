[1]: The level range is 0-7 because it fits nicely within 3 bits. The first char from the user input is stored and all but lower 3 bits are discarded to get a number between 0-7 (example: '1' == 49 == 0x31, 0x31 & 7 == 1). rest of the input (like the new line character) will flushed in the `ioctl` syscall

[2]: Putting a border so that seperate bounds check is unnecessary
